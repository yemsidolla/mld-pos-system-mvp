# Authentik stack environment (copy to .env.authentik — never commit the real file)
# Generate secrets:  openssl rand -base64 48

AUTHENTIK_TAG=2025.6
AUTHENTIK_HOST_PORT=9000

AUTHENTIK_SECRET_KEY=change-me-openssl-rand-base64-48
AUTHENTIK_PG_DB=authentik
AUTHENTIK_PG_USER=authentik
AUTHENTIK_PG_PASS=change-me-strong-password
